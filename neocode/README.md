# neocode

纯本地命令行 + 随需调用本地化 LLM 的最小可行实现（MVP）

- 目标：在本地 CLI 里接收自然语言描述，通过提示词组装调用 LLM，得到文本描述和对本地文件的改动指令，然后将改动落地。远程后端仅在明确需要时才被调用（本地 Mock 模式为默认）。
- 当前实现为一个最简骨架，提供以下模块：REPL、LLM 客户端抽象、简易文件写入/备份、以及对编辑指令的落地执行。

后续计划：增强交互性、增加离线模型、改善回滚、引入 TUI。
